apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-app-deployment
  labels:
    app: messaging-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: messaging-app
  template:
    metadata:
      labels:
        app: messaging-app
    spec:
      containers:
        - name: messaging-app
          image: messaging_app:latest
          ports:
            - containerPort: 8000
          env:
            - name: DJANGO_SECRET_KEY
              value: "fake-key-change-in-prod"
            - name: DJANGO_DEBUG
              value: "True"
            - name: MYSQL_DATABASE
              value: "messaging_db"
            - name: MYSQL_USER
              value: "django_user"
            - name: MYSQL_PASSWORD
              value: "secure_password"
            - name: MYSQL_HOST
              value: "db"
            - name: MYSQL_PORT
              value: "3306"

---

apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  selector:
    app: messaging-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
